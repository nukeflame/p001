/**
 * Settings - index
 */

import actions from "./actions";
import mutations from "./mutations";
import getters from "./getters";

const state = {
  genSettings: [],
  upadatingSettings: false
};

export default {
  state,
  actions,
  mutations,
  getters
};
